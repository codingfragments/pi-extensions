# Details

Back to the [handbook](./toc.md).

## Section one

| Column | Value | Note |
|---|---|---|
| alpha | 1 | tables should survive |
| beta | 2 | with alignment |

## Section two

A fenced code block:

```ts
export function hello(name: string): string {
  return `hello ${name}`;
}
```

A list with nesting:

- first
  - nested
- second

> A blockquote, for good measure.
